library(shiny)
library(dplyr)
library(ggplot2)

# Define UI for app that can generate basic plots from Wildlife Conflict article coding
ui <- fluidPage(
  titlePanel("Wildlife Conflict Data Entry"), 
  sidebarLayout(
    sidebarPanel(
      s
    )
  )
)